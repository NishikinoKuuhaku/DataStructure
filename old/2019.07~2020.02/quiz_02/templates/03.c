#include <stdio.h>

? calc(char *base, char *insert, int pos) // ? 处需要填入你认为正确的变量类型
{
    /* Code Here */
}

int main(int argc, char const *argv[])
{
    int n = 5 x = 1000000012;
    int a[] = {5, 4, 3, 0, 2, 1};

    ? result = insert(base, insert, pos); // ? 处需要填入你认为正确的变量类型

    printf("?????", result); // ? 处需要填入你认为正确的变量类型格式化文字
    return 0;
}
